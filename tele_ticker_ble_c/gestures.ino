void checkGestures() {
  //    if (upsideDown && millis() - upsideDownStart > 2000) {
  //      mode = SLEEP;
  //    if ( millis() - awakeStart > 5000) {
  //      if (z > 6 && !busyZ) {
  //        busyZ = true;
  //        mode ++;
  //        if (mode > 8 ) mode = 0;
  //      }
  //      if (z < -6 && !busyZ) {
  //        mode --;
  //        busyZ = true;
  //        if (mode < 0) mode = 8;
  //      }
  //    }
}

